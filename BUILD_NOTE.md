# Public site build

The GitHub Pages site currently provides four public-facing routes: `/`, `/tools/`, `/solutions/`, and `/contact/`. The site is intentionally static; public repositories remain the distribution point for source, while commercial implementation is handled separately.